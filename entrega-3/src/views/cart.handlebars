<div class="max-w-4xl mx-auto p-6 bg-white shadow-lg rounded-lg mt-10">
    <h1 class="text-3xl font-bold text-gray-800 mb-4">Detalle de la Compra</h1>
    <div class="space-y-4">
        {{#each cart.products}}
            <div class="flex items-center justify-between bg-gray-50 p-4 rounded-lg">
                <div class="flex items-center space-x-4">
                    <img src="https://via.placeholder.com/80" class="rounded-lg">
                    <div>
                        <h2 class="text-lg">{{product}}</h2>
                    </div>
                    <div>
                        <p class="text-gray-600">${{price}}</p>
                    </div>
                    <div class="flex items-center space-x-4">
                        <button class="px-3 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600">➖</button>
                        <span class="text-lg">{{quantity}}</span>
                        <button class="px-3 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600">➕</button>
                    </div>
                </div>
                <button class="text-red-500 hover:text-red-600">🗑️</button>
            </div>
        {{/each}}
    </div>
    <div class="mt-6 flex justify-between items-center text-lg font-semibold">
        <span>Total:</span>
        <span class="text-green-500">$44.98</span>
    </div>
    <div class="mt-6 flex space-x-4">
        <button class="w-1/2 px-6 py-3 bg-red-500 text-white rounded-lg hover:bg-red-600">
            Cancelar Compra
        </button>
        <button class="w-1/2 px-6 py-3 bg-green-500 text-white rounded-lg hover:bg-green-600">
            Finalizar Compra
        </button>
    </div>
</div>